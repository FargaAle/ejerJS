
function paresEntre (x,y){
    var res = x % 2
            if (res != 0) {
            x++
                    }
    for (var pares = x ; x <= y ; x= x+2)
    console.log (x)
                }

window.onload = paresEntre (32,321)

/*var x= 1,y= 31;


function paresEntre (){
    var res = x % 2
            if (res != 0) {
            x++
                    }
    for (var pares = x ; x <= y ; x= x+2)
    console.log (x)
}

window.onload = paresEntre
*/


/*Crear una funcion que dado un interval entre x e y muestre todo los numeros pares
function pares(x, y) {
    // código aqui
   }
   pares(32, 321); 
   */